# Dependabot Configuration - 2025 Best Practices
# Optimized for pnpm monorepo with Python services and Docker

version: 2

# Enable grouped updates to reduce PR noise
enable-beta-ecosystems: true

updates:
  # ==================== NPM/PNPM ====================

  # Root entry — covers the ENTIRE pnpm workspace (apps/*, packages/*, agent,
  # services/*) and the single root pnpm-lock.yaml.
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
    groups:
      # Group all non-major updates together
      production-dependencies:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
      # Separate group for dev dependencies
      development-dependencies:
        dependency-type: "development"
        update-types:
          - "minor"
          - "patch"
    # Allow all updates (remove tinacms restriction)
    open-pull-requests-limit: 10
    labels:
      - "dependencies"
      - "npm"
      - "root"
    commit-message:
      prefix: "chore(deps)"
      include: "scope"

  # NOTE: do NOT add per-member npm entries for workspace packages (e.g.
  # directory: "/agent"). `agent` and `services/*` are pnpm workspace members
  # sharing the single root pnpm-lock.yaml, which the root entry above already
  # maintains. A sub-directory entry updates only that member's package.json
  # and cannot touch the root lockfile, so CI's `pnpm install --frozen-lockfile`
  # fails with ERR_PNPM_OUTDATED_LOCKFILE and the PR can never merge.
  # (The old "/agent" and "/services/yjs-collaboration-server" npm entries were
  # removed for exactly this reason.)

  # ==================== DOCKER ====================
  #
  # NOTE: each entry must point at a directory that CONTAINS a Dockerfile.
  # Dependabot's docker ecosystem only bumps Dockerfiles and Kubernetes
  # manifests — it does NOT update `image:` pins in docker-compose*.yml. A
  # root entry (directory: "/") fails with "No Dockerfiles found" because the
  # repo root has only compose files. The compose image pins (postgres, neo4j,
  # minio, cloudflared, ...) are therefore bumped by hand.

  # Web app Dockerfiles (apps/rabbit-hole/Dockerfile + Dockerfile.fast)
  - package-ecosystem: "docker"
    directory: "/apps/rabbit-hole"
    schedule:
      interval: "weekly"
      day: "thursday"
      time: "09:00"
    groups:
      docker-app:
        patterns:
          - "*"
    open-pull-requests-limit: 3
    labels:
      - "dependencies"
      - "docker"
      - "web"
    commit-message:
      prefix: "chore(deps-docker-app)"

  # Agent Dockerfiles
  - package-ecosystem: "docker"
    directory: "/agent"
    schedule:
      interval: "weekly"
      day: "thursday"
      time: "09:00"
    groups:
      docker-agent:
        patterns:
          - "*"
    open-pull-requests-limit: 2
    labels:
      - "dependencies"
      - "docker"
      - "agent"
    commit-message:
      prefix: "chore(deps-docker-agent)"

  # Job-processor service Dockerfile
  - package-ecosystem: "docker"
    directory: "/services/job-processor"
    schedule:
      interval: "weekly"
      day: "thursday"
      time: "09:00"
    groups:
      docker-job-processor:
        patterns:
          - "*"
    open-pull-requests-limit: 2
    labels:
      - "dependencies"
      - "docker"
      - "services"
    commit-message:
      prefix: "chore(deps-docker-job-processor)"

  # Custom Postgres image (docker/postgres/Dockerfile — pgvector base)
  - package-ecosystem: "docker"
    directory: "/docker/postgres"
    schedule:
      interval: "weekly"
      day: "thursday"
      time: "09:00"
    groups:
      docker-postgres:
        patterns:
          - "*"
    open-pull-requests-limit: 2
    labels:
      - "dependencies"
      - "docker"
      - "database"
    commit-message:
      prefix: "chore(deps-docker-postgres)"

  # ==================== GITHUB ACTIONS ====================

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
      day: "monday"
      time: "09:00"
    groups:
      github-actions:
        patterns:
          - "*"
    open-pull-requests-limit: 3
    labels:
      - "dependencies"
      - "github-actions"
      - "ci"
    commit-message:
      prefix: "chore(deps-ci)"
