"use client";

/**
 * EntityReviewInterrupt — disabled (M4 milestone: CopilotKit/LangGraph removed)
 *
 * Will be re-implemented using the native deep research API in a follow-up milestone.
 */

// EntityReviewInterrupt is disabled — CopilotKit/LangGraph removed (M4 milestone).
export function EntityReviewInterrupt() {
  return null;
}

